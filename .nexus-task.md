You are editing a portfolio company app — a Turborepo monorepo at
`apps/web/` that already has the Nexus substrate pre-installed.

### The substrate is PRE-INSTALLED and read-only

Every portfolio company ships on the same substrate (per chairman
2026-05-12: "one consistent architecture, copied not re-coded"). At
provisioning time, `templates/portfolio-substrate/` + all 16 legos
were copied into this repo as an initial commit (look for the commit
message `[substrate] initial install`). The substrate provides:

   1. @nexus/identity-and-access          — auth, sessions, OAuth, MFA, password reset
   2. @nexus/profile-and-account          — profile editor + connected accounts + GDPR account export
   3. @nexus/organizations-and-teams      — multi-tenant orgs + invitations + RBAC + seats + audit log
   4. @nexus/billing-and-subscriptions    — Stripe checkout + portal + plans + usage + dunning
   5. @nexus/legal-and-compliance         — ToS / Privacy / Cookies + acknowledgments
   6. @nexus/notifications                — transactional email + in-app + SMS + push
   7. @nexus/support-and-help             — KB + ticketing + AI deflection + agent first-line support (triage/draft/escalate)
   8. @nexus/search                       — global cmd+K + faceted filters + saved searches + agent re-ranking
   9. @nexus/onboarding                   — welcome flow + tour + checklist + sample data + agent path/dropoff
  10. @nexus/files-and-media              — upload + scan + signed URLs + agent categorize/extract/flag
  11. @nexus/admin-console                — sections + feature flags + system config + audit
  12. @nexus/developer-surface            — API keys + webhooks + rate limit + agent usage/anomaly monitoring
  13. @nexus/analytics-and-telemetry      — events + funnels + retention + agent metric/funnel/retention analysis
  14. @nexus/social-and-engagement        — comments + reactions + referrals + agent moderation/sentiment
  15. @nexus/crm-and-lifecycle            — contacts + leads + pipeline + outreach + agent-run scoring/drafting
  16. @nexus/memory-and-knowledge         — agent runtime memory tiers + knowledge compiler

DO NOT recreate any of these features. Each lego is a workspace
package importable from `@nexus/<lego-name>` (e.g.
`import { getServerSession } from "@nexus/identity-and-access"`).
Substrate-generated API shims under `apps/web/app/api/<lego>/` wrap
each lego's handlers — those shims are substrate-owned too.

### Your carve-out

Your work lives ONLY in these three areas:

  - `apps/web/app/(<domain>)/*`           — your domain's pages + route
                                            handlers. `<domain>` is the
                                            carve-out segment named in
                                            your task contract (e.g.
                                            `(proposals)`, `(sdr)`,
                                            `(compliance)`).
  - `apps/web/lib/<domain>/*`             — your domain's server-side
                                            logic (DB queries, API
                                            clients, business code).
  - `packages/db/company/<domain>.ts`     — your domain's Drizzle table
                                            definitions. The substrate's
                                            `packages/db/index.ts` is
                                            wired to concatenate
                                            `packages/db/company/*.ts`
                                            into the schema at build
                                            time.

### Company tables MUST be declared in packages/db/company/<domain>.ts

Every database table your code queries MUST have its `CREATE TABLE IF NOT
EXISTS` DDL in `packages/db/company/<domain>.ts`, exported as a string
constant whose name ends in `_DDL` (e.g. a `CABINETS_DDL` constant holding
the CREATE TABLE statements).
`packages/db/migrate.ts` runs every `*_DDL`/`*_SCHEMA_SQL` constant against
the company database at deploy, and the integration TABLE-REF gate ONLY
recognizes tables created there (or in a lego's schema).

Do NOT create tables with an inline `CREATE TABLE` buried inside
`apps/web/lib/**`, a page, or a route handler — migrate.ts never sees it,
so the table won't exist at deploy (the app 500s at runtime) and the build
is REJECTED ("references a base table with no creating DDL"). One
`<domain>.ts` per company domain; add EVERY table you own to it, and make
the table name match exactly what your queries reference.

URL-determinism exceptions (external services call fixed paths, so
these escape the route-group requirement):

  - `apps/web/app/api/webhooks/*`         — external callbacks (Resend,
                                            Calendly, Stripe webhooks
                                            for company-specific events
                                            not owned by the billing lego)
  - `apps/web/app/api/unsubscribe/*`      — CAN-SPAM unsubscribe
  - `apps/web/app/api/cron/*`             — Vercel cron handlers

### Forbidden — DO NOT MODIFY

Substrate root files:
  - `apps/web/app/page.tsx`        — root = conversation surface. If your
                                     task feels like it requires editing
                                     this file, STOP and surface the
                                     conflict in your response. It is
                                     NEVER the right move.
  - `apps/web/app/layout.tsx`      — root layout (theme, fonts, providers).
  - `apps/web/app/globals.css`     — substrate global CSS.

Substrate surface routes (per spec §6 — the 5 surfaces the agent runtime
serves):
  - `apps/web/app/approval/`       — §6.4 approval surface
  - `apps/web/app/artifact/`       — §6.3 artifact surface
  - `apps/web/app/conversation/`   — §6.1 conversation surface
  - `apps/web/app/direct/`         — §6.5 direct CRUD fallback
  - `apps/web/app/work/`           — §6.2 work surface

Substrate lego-owned API shims (each lego's handlers are wrapped here):
  - `apps/web/app/api/auth/*`,
    `apps/web/app/api/admin/*`,
    `apps/web/app/api/billing/*`,
    `apps/web/app/api/stripe/*`,
    `apps/web/app/api/notifications/*`,
    `apps/web/app/api/legal/*`,
    `apps/web/app/api/memory/*`    — lego-owned shim namespaces.
  - `apps/web/app/api/health/`     — substrate health probe (deploy-monitor reads it).

Substrate UI + runtime + workspace packages:
  - `apps/web/components/*`        — substrate UI primitives (import via
                                     `@/components/*` tsconfig alias;
                                     never edited).
  - `apps/web/lib/legos.config.ts` — generated by the installer; never
                                     hand-edit.
  - `apps/web/lib/runtime.ts`      — substrate runtime client.
  - `packages/db/*` (except `company/<domain>.ts`) — Drizzle entry;
                                     substrate-owned.
  - `packages/ui/`,
    `packages/runtime-client/`     — substrate workspace packages.
  - `legos/`                       — every lego is substrate source-of-
                                     truth; edits go via a separate
                                     chairman lego sprint.

Substrate infrastructure:
  - `apps/web/next.config.js`,
    `apps/web/next-env.d.ts`,
    `apps/web/tsconfig.json`,
    `next.config.js`, `turbo.json`,
    `tsconfig.json`, `vercel.json`,
    `package.json` (root),
    `package-lock.json`            — substrate infrastructure. Off-limits.

`apps/web/package.json` is a HALF-exception: you MAY add new
dependencies in the same commit as the import that uses them. NEVER
remove or downgrade an existing dependency unless your task contract
explicitly authorizes it.

The forbidden list above is illustrative, not exhaustive. The actual
rule is simple: **a path is allowed if and only if it matches your
carve-out (the 3 allowed areas + 3 API exceptions + `apps/web/package.json`).
If a path doesn't match, don't touch it.** When the substrate adds
new files in future versions, they're implicitly forbidden — you
don't need a list update to know to leave them alone.

### Repo structure anchor

This IS a Turborepo monorepo (unlike the Nexus repo itself). The
structure post-install:

  - `apps/web/`                    — the Next.js 14 app (where your work lives)
  - `apps/web/app/`                — Next.js App Router. Route groups
                                     `(<domain>)/` are valid; the parens
                                     don't affect URLs, just file
                                     organization.
  - `apps/web/app/(<domain>)/`     — YOUR carve-out (pages + route handlers)
  - `apps/web/lib/`                — substrate runtime helpers
                                     (`legos.config.ts`, `runtime.ts`) +
                                     YOUR domain code under `<domain>/`
  - `apps/web/components/`         — substrate UI primitives (read-only;
                                     import via `@/components/*`)
  - `packages/db/`                 — substrate Drizzle entry (read-only)
  - `packages/db/company/`         — YOUR domain's Drizzle tables
  - `packages/ui/`,
    `packages/runtime-client/`     — substrate workspace packages (read-only)
  - `legos/<lego-name>/`           — each lego: api/, ui/, schema/,
                                     admin/, agent/, index.ts (read-only;
                                     importable as `@nexus/<lego-name>`)

`@/*` is the substrate's tsconfig path alias declared in
`apps/web/tsconfig.json:compilerOptions.paths` pointing at
`apps/web/*`. It is NOT an npm package — it's a compile-time alias.
Imports like `@/components/conversation/surface` resolve to local
files; do NOT add them to `apps/web/package.json`.

### UI conventions — the substrate styles your markup automatically

`apps/web/app/globals.css` (substrate-owned, do NOT edit) ships a
functional layout baseline: a centered padded container on `<main>`
plus element-level styling for headings, paragraphs, forms, inputs,
selects, buttons, tables, and lists — all SCOPED under `main`. So the
plain semantic HTML you emit renders user-friendly with ZERO extra
work. Write real elements (`<h1>`, `<p>`, `<form>`, `<input>`,
`<select>`, `<button>`, `<table>`, `<ul>`) rather than `<div>` soup,
and they look finished. Do NOT hand-roll your own padding/margins or
inline a full stylesheet to fight the baseline — the container and
element defaults already handle spacing and gutters.

On data-heavy pages, reach for these substrate helper classes (all
defined in globals.css; monochrome + dark-mode-safe):

  - `class="toolbar"`  — wrapper of a search/filter row (input +
                         selects + a Search button); lays them out in a
                         horizontal, wrapping, gap-spaced row instead of
                         jamming controls together.
  - `class="card"`     — a bounded object (a record, a summary panel):
                         bordered, rounded, padded surface.
  - `class="empty"`    — an empty-state block ("No items yet. Add your
                         first…"): a calm, centered, dashed-border panel.
  - `class="muted"`    — secondary / hint text.
  - `class="btn"` /
    `class="btn secondary"` — style an `<a>` as a primary / secondary
                         button (matches the default `<button>` look).

Standard page shape to follow: an `<h1>` title, ONE lead `<p>`
description directly under it (auto-styled as a muted subtitle), the
primary action, an optional `toolbar` filter row, then the list/table
of results OR an `empty` block when there's no data.

### Other rules

  1. Every npm import in your code MUST also be declared in
     `apps/web/package.json:dependencies` in the SAME commit. Running
     `npm install <pkg>` mid-task without persisting to package.json
     is a known footgun — the local `node_modules/` is ephemeral;
     Vercel's fresh clone won't have it. Cycle 10 F1-001 evidence
     (May 13).

  2. Server-side native modules (`pg`, `puppeteer`, `pdf-parse`,
     `mammoth`) require BOTH the package in
     `apps/web/package.json:dependencies` AND either a
     `/* webpackIgnore: true */` directive on the dynamic import OR an
     inline `require(...)` wrapped so Next.js doesn't try to bundle it
     for the client. The substrate's `next.config.js` is forbidden, so
     `serverExternalPackages` isn't available — use the inline-comment
     or `eval("require")` pattern instead.

  3. TypeScript path aliases (`@/...`, `@components/...`) are NOT npm
     packages. They resolve via `tsconfig.json:compilerOptions.paths`
     to local directories. Don't add them to `package.json`.

  4. If the task contract names a specific carve-out segment (e.g.
     `(proposals)`), every file you create MUST live inside the three
     carve-out areas (`apps/web/app/(<domain>)/`, `apps/web/lib/<domain>/`,
     `packages/db/company/<domain>.ts` or `<domain>/`) or in one of the API-exception
     prefixes. Files anywhere else belong to legos or the substrate —
     they're off-limits.

  5. Use `Write` to create new files inside the carve-out; use `Edit`
     to modify existing files. Both tools are available in portfolio
     mode (unlike self-build mode where Write is intentionally
     disabled). Most portfolio tasks REQUIRE Write because the
     feature is new domain code, not a tweak to existing substrate.

  6. Type hints on all new function signatures (TypeScript / Python).
     Structured JSON logs (no `console.log` left behind). Use
     `process.env.X` for env vars — never hardcode keys.

### Read the substrate before you write

Before authoring new files, read these substrate files to align with
existing conventions (verified to exist 2026-05-13):

  - `apps/web/components/conversation/surface.tsx`  — the substrate's
    conversation UI primitive you'll likely import for new pages.
  - `packages/db/index.ts`                           — Drizzle entry;
    documents how `packages/db/company/<domain>.ts` plugs into the
    substrate schema.
  - `legos/identity-and-access/index.ts`             — example lego
    barrel export; shows the import pattern
    `import { X } from "@nexus/identity-and-access"`.
  - `legos/identity-and-access/api/login.ts`         — example lego
    API handler shape (called by the substrate's
    `apps/web/app/api/auth/login/route.ts` shim).
  - `apps/web/lib/legos.config.ts`                   — generated wiring;
    READ to understand which legos are active for this company. NEVER
    edit (regenerated by the installer).

Your code lives alongside well-established conventions. Match them.


---

# Nexus Coding Task Specification

**Task ID:** cd2eec67-4551-4240-927c-39fddb0874bc
**Feature ID:** CR-001
**Branch:** change/7fad00b5-on-the-homepage-add-a-short-subheadline
**Complexity:** complex
**Attempt:** 1

## Quality Requirements

The reviewer scores your work 0-100. **You need ≥70 to pass.**
- 90-100: All criteria met, complete implementations, clean code
- 70-89: Mostly complete, minor issues only
- 50-69: Partial completion or stub implementations → REJECTED
- <50: Missing files, `pass`/TODO bodies, unmet criteria → REJECTED

**Every function body must contain real logic. `pass`, `...`, and `raise NotImplementedError` are FORBIDDEN.**

## Tech Stack

Python 3.12 · asyncio · asyncpg (raw SQL, NO ORM) · NATS JetStream · FastAPI
Use `async def` for all I/O. Use `asyncpg` connection pools. Match patterns in reference snippets.

## Objective

CHANGE REQUEST against the LIVE deployed app for CondoCentral (repo NexusaiHoldings/condocentral). The substrate + domain features are already built and in production — make the SMALLEST change that satisfies the request; do not refactor or rebuild anything else.

Chairman request: User feedback (edit): On the homepage, add a short subheadline directly under the main hero title that reads: "Self-management made simple for every HOA." Keep all other content and styling the same.

Scope (change-request mode): you MAY edit any file under apps/web/ including shared substrate files (globals.css, components) when the fix genuinely belongs there. You MUST NOT touch: apps/web/lib/theme/active-theme.ts, apps/web/lib/home/home-config.ts, apps/web/app/api/auth/*, legos.config.ts, or anything under packages/ or legos/. A human reviews the full diff plus a live preview deployment before anything reaches production.

## File Scope (HARD BOUNDARY)


**DO NOT touch any files not listed above.**

## Acceptance Criteria

- [ ] User feedback (edit): On the homepage, add a short subheadline directly under the main hero title that reads: "Self-management made simple for every HOA." Keep all other content and styling the same.

## Constraints

- Python 3.12+: async/await throughout, type hints on all function signatures
- SQL: parameterized queries ($1, $2) only — never string interpolation
- All IDs are UUID — never integer primary keys
- No banned packages: @supabase/supabase-js, openai (in company apps), drizzle-orm, ioredis, pg-native
- Use gpt-5.4-mini if referencing OpenAI models (NOT gpt-4o-mini)
- Next.js: server components by default — 'use client' only for interactivity
- No CHECK constraints on free-form text columns
- Commit format: feat(feature_id): description
- Do NOT touch files outside your file scope — reviewer will auto-reject

## Pre-Commit Checklist

Before staging and committing, verify each item below is satisfied in your code:

- [ ] User feedback (edit): On the homepage, add a short subheadline directly under the main hero title that reads: "Self-management made simple for every HOA." Keep all other content and styling the same.

If ANY item is unchecked, fix it before committing. The reviewer checks every criterion.

## Output Instructions

1. Implement ALL assigned files with complete, working code (no stubs)
2. Verify the Pre-Commit Checklist above is fully satisfied
3. Stage ONLY your assigned files and commit: `git add -A && git commit -m 'feat(CR-001): <description>'`
   **IMPORTANT:** Do NOT use `git add .` or `git add -A`. Only add the files listed in CREATE and MODIFY above.
4. Do NOT push — integration worker handles that


## KNOWN FAILURE PATTERNS — AVOID THESE

These patterns have been observed repeatedly across past sprints.
Code that triggers them WILL fail review or build. Read carefully:

  1. [banned_package] Banned package @supabase/ssr: use NextAuth (already provisioned with NEXTAUTH_SECRET/NEXTAUTH_URL)
  2. [banned_package] Banned package @eslint/eslintcompat: remove — known to cause CRA/Vite build failures
  3. [banned_package] Banned python import openai: use the gateway proxy — never call the openai SDK directly
  4. [banned_package] Banned package @supabase/auth-helpers: use NextAuth (already provisioned with NEXTAUTH_SECRET/NEXTAUTH_URL)
  5. [banned_package] Banned package pg-native: remove — incompatible with Vercel serverless; use connection-pooling pg without native
  6. [banned_package] Banned package @supabase/supabase-js: use NextAuth (already provisioned with NEXTAUTH_SECRET/NEXTAUTH_URL)
  7. [banned_package] Banned package libpq: remove — same reason as pg-native
  8. [banned_package] Banned python import supabase: use raw psycopg/asyncpg via DATABASE_URL
  9. [banned_package] Banned package drizzle-orm: use raw SQL via the provisioned DATABASE_URL
  10. [banned_package] Banned package ioredis: Redis is not in the provisioning stack — drop the dep or replace with in-memory cache

If your generated code would exhibit any of the above, fix it BEFORE committing.